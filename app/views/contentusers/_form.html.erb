<%= form_for(@contentuser) do |f| %>
  <% if @contentuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contentuser.errors.count, "error") %> prohibited this contentuser from being saved:</h2>

      <ul>
      <% @contentuser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :word_id %><br />
    <%= f.number_field :word_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
